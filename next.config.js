/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : ['upcdn.io', 'localhost', 'replicate.delivery'] // <== Domain name
      }
}

module.exports = nextConfig
